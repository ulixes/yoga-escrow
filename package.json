{
  "name": "yoga-escrow-monorepo",
  "private": true,
  "packageManager": "bun@1.1.14",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "format": "turbo run format"
  },
  "devDependencies": {
    "turbo": "^2.0.6",
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "@privy-io/js-sdk-core": "^0.53.3"
  }
}
